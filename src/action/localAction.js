import { createAction } from '@reduxjs/toolkit';

export const checkDarkmode = createAction('local/CHECK_DARKMODE');
export const sidebarIcon = createAction('local/SIDEBAR_ICON');
export const hamburgerIcon = createAction('local/HAMBURGER_ICON');
export const navOpen = createAction('local/NAV_OPEN');
